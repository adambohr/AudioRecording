name: sonohaler_lab
description: Sonohaler Lab - Raw acoustic data collection for inhaler research.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record: 5.0.0
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  path: ^1.9.0
  permission_handler: ^11.3.0
  csv: ^6.0.0
  uuid: ^4.3.3
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
